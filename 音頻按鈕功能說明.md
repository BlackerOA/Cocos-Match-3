# 音頻按鈕功能說明

## ✨ 功能特點

根據您的需求，音頻按鈕現在具有以下特性：

### 1. 場景獨立控制
- **Login場景的音頻按鈕**：只控制Login場景的背景音樂
- **Game場景的音頻按鈕**：只控制Game場景的背景音樂
- 兩個場景的按鈕狀態**完全獨立**，互不影響

### 2. 預設狀態
- 所有場景的音樂按鈕**預設為開啟狀態**
- 首次進入任何場景時，音樂都會自動播放

### 3. 會話內記憶
- 在同一個遊戲會話中（未刷新網頁的情況下）：
  - Login場景會記住自己最後的靜音狀態
  - Game場景會記住自己最後的靜音狀態
  - 例如：在Login關閉音樂 → 進入Game（音樂開啟）→ 返回Login（音樂仍然關閉）

### 4. 刷新重置
- **F5刷新網頁**或**重新開啟遊戲**後：
  - 所有場景的音樂按鈕都會**重置為預設開啟狀態**
  - 不會記住上次的設定

### 5. 場景切換時的音樂處理
- 從Login切換到Game時：
  - Login的音樂會自動停止
  - Game的音樂會根據Game場景的靜音狀態決定是否播放
- 從Game返回Login時：
  - Game的音樂會自動停止
  - Login的音樂會根據Login場景的靜音狀態決定是否播放

## 🎮 使用範例

### 範例 1：基本使用
```
1. 打開遊戲 → Login場景音樂播放（預設開啟）
2. 點擊Login的音頻按鈕 → Login音樂停止
3. 進入Game場景 → Game音樂播放（預設開啟，不受Login影響）
4. 點擊Game的音頻按鈕 → Game音樂停止
5. 返回Login場景 → Login音樂保持停止（記住之前的狀態）
```

### 範例 2：刷新重置
```
1. Login場景，點擊按鈕關閉音樂
2. 進入Game場景，點擊按鈕關閉音樂
3. F5刷新網頁
4. Login場景音樂自動播放（重置為預設開啟）
5. 進入Game場景，音樂自動播放（重置為預設開啟）
```

## 🔧 技術實現

### GlobalAudioManager 改進
- 使用 `_audioSources` 物件存儲每個場景的音頻狀態
- 每個場景有獨立的：
  - `audioId`：音頻播放ID
  - `isMuted`：靜音狀態（預設 false）
  - `clip`：音頻資源

### API 使用方式
```javascript
// 播放音樂（需指定場景名稱）
GlobalAudioManager.playBGM('Login', audioClip, true, 1);
GlobalAudioManager.playBGM('Game', audioClip, true, 1);

// 切換靜音（指定場景）
GlobalAudioManager.toggleMute('Login');
GlobalAudioManager.toggleMute('Game');

// 檢查靜音狀態（指定場景）
GlobalAudioManager.isMuted('Login');
GlobalAudioManager.isMuted('Game');
```

## 📝 注意事項

1. **場景名稱必須一致**：
   - LoginController 使用 `'Login'`
   - GameController 使用 `'Game'`
   - 確保字串大小寫一致

2. **記憶方式**：
   - 使用 JavaScript 記憶體存儲（不使用 localStorage）
   - 刷新後自動清除，符合您的需求

3. **音樂切換**：
   - 場景切換時會自動停止其他場景的音樂
   - 確保同一時間只有一個音樂在播放

## ✅ 完成的修改

1. ✅ [GlobalAudioManager.js](assets/Script/Utils/GlobalAudioManager.js) - 完全重寫支援多場景
2. ✅ [LoginController.js](assets/Script/Controller/LoginController.js) - 更新使用場景名稱
3. ✅ [GameController.js](assets/Script/Controller/GameController.js) - 更新使用場景名稱
4. ✅ 預設狀態設為開啟
5. ✅ 刷新後重置功能
6. ✅ 場景獨立控制

現在您的遊戲音頻控制應該完全符合您的需求了！
